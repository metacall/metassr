<!--
  Example-app PR template. Opt-in only: pick it with the `template` query
  parameter (`gh pr create --template example` or `?template=example.md`).
  It does NOT auto-apply to every PR in the repo.

  This is a community project — write the idea below in YOUR OWN words. No AI
  boilerplate: the maintainers and other builders want to know what YOU made
  and why. Fill every section. See AGENTS.md.
-->

## Project idea

<!-- What is your app? What does it demonstrate? In your own words. -->

<your app, in one or two sentences>

## How to run it

```sh
gh pr checkout <PR_NUMBER>
cd <project-name>
npm install   # first time
npm run dev
```

Open http://localhost:3000.

<!-- Anything unusual about the setup (extra dependencies, a custom Dockerfile,
     environment variables)? Note it here. -->