{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "App.tsx",
  "scripts": {
    "build": "../../target/debug/metassr --debug-mode=metacall build",
    "build:ssg": "../../target/debug/metassr --debug-mode=metacall build -t ssg",
    "run": "../../target/debug/metassr --debug-mode=metacall run ",
    "run:ssg": "../../target/debug/metassr --debug-mode=metacall run --serve"
  },
  "devDependencies": {
    "@rspack/core": "^0.7.5",
    "@types/react": "^18.0.24",
    "@types/react-dom": "^18.0.8"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
