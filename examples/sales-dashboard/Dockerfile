# syntax=docker/dockerfile:1.7

# The sales-dashboard example image: the generic MetaSSR app image built with
# this example as context. The generic image already installs the example's
# Python dependencies (numpy, pandas) from requirements.txt into the bundled
# Python 3.14 runtime, so no extra steps are needed here.
#
#   docker build -f examples/sales-dashboard/Dockerfile -t sales-dashboard examples/sales-dashboard
#include ../../docker/app.Dockerfile